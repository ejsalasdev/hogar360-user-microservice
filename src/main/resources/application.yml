server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hogar360_user_db
    username: root
    password: toor
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
